# Graphiti Server Configuration Example

# Required: OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key
OPENAI_BASE_URL=https://api.openai.com/v1  # Optional, defaults to OpenAI
MODEL_NAME=gpt-4  # Optional
EMBEDDING_MODEL_NAME=text-embedding-3-small  # Optional

# Required: Neo4j Configuration
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password

# Optional: API Key Authentication (for backwards compatibility)
API_KEY=your-api-key-for-programmatic-access

# Required for OAuth: PostgreSQL User Database
POSTGRES_URI=postgresql+asyncpg://user:password@localhost:5432/graphiti_users

# Required for OAuth: JWT Configuration
JWT_SECRET_KEY=your-very-secret-key-minimum-32-characters-long
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Required for OAuth: Server URL
OAUTH_REDIRECT_BASE_URL=http://localhost:8000

# Optional: Google OAuth (leave empty to disable)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: GitHub OAuth (leave empty to disable)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional: Redis for session state (recommended for production)
REDIS_URL=redis://localhost:6379

# Production settings (uncomment for production)
# OAUTH_REDIRECT_BASE_URL=https://your-domain.com
# USE_SECURE_COOKIES=true
# CORS_ORIGINS=["https://your-frontend.com"]